<h1>Credit Creation Page</h1>

<div class="row">
	<div class="center">
	
	<%= form_for(@credit) do |f| %>

	<%= f.hidden_field :user_id, :value => current_user.id %>

	<%= f.submit "Create Credit with Unique Credit Code", class: "btn btn-large btn-primary" %>

	<% end %>

	</div>
</div>